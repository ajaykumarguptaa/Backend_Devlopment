<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>File Viewer</title>
    <style>
      /* Custom keyframes for fade and slide */
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .fade-in {
        animation: fadeIn 0.8s ease-out forwards;
      }

      /* Scrollbar styling for modern look */
      ::-webkit-scrollbar {
        width: 6px;
      }
      ::-webkit-scrollbar-thumb {
        background-color: #4f46e5;
        border-radius: 10px;
      }
    </style>
  </head>
  <body class="bg-zinc-900 text-white min-h-screen flex items-center justify-center px-4 py-8">
    <div
      class="w-full max-w-4xl bg-zinc-800 rounded-2xl shadow-2xl p-8 fade-in"
    >
      <h1
        class="text-xl font-bold text-blue-500 mb-6 border-b-2 border-zinc-600 pb-3 tracking-wide"
      >
        📄 File Name: <span class="text-white"><%=filename%></span>
      </h1>

      <div
        class="overflow-y-auto max-h-[60vh] text-gray-200 leading-relaxed text-justify space-y-4 pr-2"
      >
        <p class="hover:text-blue-300 transition duration-300 ease-in-out">
        <%=filedata%>
        </p>
      </div>

      <div class="mt-6 text-right">
        <a
          href="/"
          class="inline-block bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-2 rounded-lg transition-all duration-300 shadow-md hover:shadow-lg"
        >
          ⬅ Back to Home
        </a>
      </div>
    </div>
  </body>
</html>
