<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
     <link rel="stylesheet" href="/stylesheet/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <title>Document</title>
</head>
<body>
  <div class="h-screen w-full bg-zinc-800 flex justify-center items-center">
  <form action="/edit/<%= user._id %>" method="POST" class="max-w-md mx-auto bg-gray-200 p-6 rounded">
  <label class="block text-zinc-900">Name:</label>
  <input type="text" name="name" value="<%= user.name %>" class="w-full p-2 rounded border " required />

  <label class="block text-zinc-900 mt-4">Email:</label>
  <input type="email" name="email" value="<%= user.email %>" class="w-full p-2 rounded border " required />

  <label class="block text-zinc-900 mt-4">Image URL:</label>
  <input type="text" name="image" value="<%= user.image %>" class="w-full p-2 rounded border " required />

  <button type="submit" class="mt-6 bg-blue-500 text-white px-4 py-2 rounded">Update</button>
</form>
</div>
</body>
</html>